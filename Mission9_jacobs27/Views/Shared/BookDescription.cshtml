@{
}
@model Book
@*List items in database*@
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>
            @Model.Title
            <span class="badge-pill badge-primary" style="float:right">
                @*BOOTSTRAP changed button text color to white*@
                <small style="color:white">@Model.Price.ToString("c")</small>
</span>
        </h4>
    </div>
    <div class="card-text p-1">@Model.Author</div>
    <div class="card-text p-1">@Model.Category</div>
    <div class="card-text p-1">@Model.Classification</div>
    <div class="card-text p-1">@Model.PageCount pages</div>
    <div class="card-text p-1">@Model.Isbn</div>
    <div class="card-text p-1">@Model.BookId</div>
    @*cart functionality*@
    <form method="post" asp-page="/Cart">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name = "returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <span class="card-text p-1">
            <button type="submit" class="btn btn-success btn-sm float-end" style="float:right">
               @*BOOTSTRAP added cart icon using bootstrap*@
                <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
                <span class="glyphicon glyphicon-shopping-cart"></span>
            </button>
        </span>
    </form>
</div>


